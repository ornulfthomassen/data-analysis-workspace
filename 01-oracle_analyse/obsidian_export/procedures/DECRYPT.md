# DECRYPT

**Schema:** `CRM_ANALYSE` | **Type:** `Procedure`

## Description
Defines a PL/SQL function named `decrypt` that takes a RAW input, decrypts it using the DES algorithm with a fixed key 'starpass', and returns the decrypted value as a VARCHAR2 string, trimming any null characters.

