# IS_NUMBER

**Schema:** `CLM_ADM` | **Type:** `Procedure`

## Description
Defines a PL/SQL function named IS_NUMBER which checks if a given VARCHAR2 string can be successfully converted to a NUMBER. It returns 'TRUE' if convertible, and 'FALSE' otherwise.

