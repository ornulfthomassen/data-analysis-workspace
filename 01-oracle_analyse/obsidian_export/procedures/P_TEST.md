# P_TEST

**Schema:** `CLM_ADM` | **Type:** `Procedure`

## Description
The procedure `p_test` primarily enables the Oracle `DBMS_OUTPUT` buffer. It does not perform any data manipulation (INSERT, UPDATE, DELETE) or data retrieval (SELECT) from user-defined tables or views. Note: There is a typo `DBMS _OUTPUT` in one line that would likely cause a compilation error.

