# GET_BIND_GROUPS

**Schema:** `CRM_ANALYSE` | **Type:** `Procedure`

## Description
Categorizes a given number of days into predefined ranges and returns a descriptive string for that range. It acts as a lookup function for aging or grouping based on a 'DAYS' input.

