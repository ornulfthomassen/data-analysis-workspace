# KIM_FROM_ORDER_PRODUCT_DIM_V

**Schema:** `CCM` | **Type:** `View`

## Description
This view is a direct projection or an alias of an existing product dimension view from the 'GALAXY' schema. It selects all columns from the source view 'GALAXY.FROM_ORDER_PRODUCT_DIM_V' without any transformations, filtering, or joins. Its primary purpose appears to be to expose the product dimension data to the 'CRM_ANALYSE' schema, possibly for reporting or analytical purposes under a specific naming convention or access control, acting as a shim or a mirror of the original view.

## Data Sources (Inputs)
- ← [[GALAXY.FROM_ORDER_PRODUCT_DIM_V]]

